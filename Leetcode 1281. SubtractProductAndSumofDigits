import math

class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        
      p=1
      s=0
    
      while(n>0):
        
        a=n%10
        p=p*a
        s=s+a
        n=math.floor(n/10)
    
      return p-s

#another approach- cover no to str and iterate, return aback to int and add or take product

# suma=0
# prod=1

# str1=str(121)
    
# for i in str1:
    
#     suma=suma+int(i)
    
#     prod= prod*int(i)

# print (prod, suma)
    
